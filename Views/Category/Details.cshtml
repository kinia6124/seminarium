@model seminarium.Models.Category

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nazwa)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nazwa)
        </dd>
        </br>
        </br>
        <dt>
            @Html.DisplayNameFor(model => model.CategoryBooks)
        </dt>
        </br>

        <dd>
            <table class="<table">
                <tr>
                    <th>Tytuł Książki</th>
                </tr>
                @foreach (var item in Model.CategoryBooks)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Book.Tytul)</td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
